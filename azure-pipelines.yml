trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: UseNode@2
  inputs:
    version: '18.x'

- script: |
    npm install
    npm run build || echo "No build step, skipping"
  displayName: 'Instalar dependencias y construir app'

- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(System.DefaultWorkingDirectory)'
    includeRootFolder: false
    archiveType: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
    replaceExistingArchive: true

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'

- task: AzureWebApp@1
  inputs:
    azureSubscription: 'PlusPetrolConnection' # Nombre de tu conexión de servicio
    appType: 'webApp'
    appName: 'app-test-01-d6ggcpb7g3e9etfv' # Nombre de tu App Service
    package: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
